import { AnimatePresence } from 'framer-motion'
import React from 'react'

const PageTransition = ({children}) => {
  return (
    <AnimatePresence>PageTransition</AnimatePresence>
  )
}

export default PageTransition